edit
<%= form_with model: @user do |t| %>
    Username<%= t.text_field :username %><br /><br />
    Email<%= t.text_field :email %>
    password<%= t.text_field :password %>
    <%= t.submit "submit" %>
    <% end %>